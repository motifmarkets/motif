{
    "extends": "../../tsconfig.app.json",

    "compilerOptions": {
        "composite": true
    },

    "include": [
        "**/*.ts"
    ],

    "exclude": [
        "scheduler.ts", // possible future use
    ],

    "references": [
        { "path": "../res" }
    ]
}