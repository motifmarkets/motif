import { LayoutConfig } from 'golden-layout';

export class Layout {
    name: string;
    schemaVersion: string;
    description: string;
    versionId: string;
    hash: number;
    golden: LayoutConfig;
}
